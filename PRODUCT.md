# Product

## Register

product

## Users

个人用户，在重复登录、重复填表或测试多个网页流程时，希望用少量配置复用自己的本地凭据和页面操作。

## Product Purpose

AutoFill Studio 是一个本地优先的 Chrome 扩展，用站点规则描述网页元素、填充值和触发动作。用户可以为不同网站创建配置，点选页面元素后保存规则，并按站点开关自动执行。

## Brand Personality

直接、克制、可靠。界面以配置效率为先，不用长篇说明打断操作。

## Anti-references

不做装饰性仪表盘、不使用过多渐变和大面积卡片、不把密码上传到服务器、不默认对所有网站开启自动提交。

## Design Principles

- 先显示当前页面和当前规则，再展开细节。
- 规则必须可读、可修改、可单步测试。
- 自动行为按站点启用，默认安全关闭提交动作。
- 页面拾取要有明确的高亮和撤销反馈。
- 配置失败时显示具体步骤和下一步动作。

## Accessibility & Inclusion

保持键盘可操作、焦点状态清晰、颜色不作为唯一状态提示，并支持 `prefers-reduced-motion`。密码字段使用安全文本输入，页面拾取模式支持 Escape 退出。
